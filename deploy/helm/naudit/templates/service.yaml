apiVersion: v1
kind: Service
metadata:
  name: {{ include "naudit.fullname" . }}
  labels:
    {{- include "naudit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "naudit.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
